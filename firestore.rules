rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own run data
    match /users/{userId}/runs/{runId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
